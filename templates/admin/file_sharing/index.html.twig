{% extends 'layout/admin.html.twig' %}

{% block pageTitle %}{{ 'Dosya Paylaşımı'|trans }}{% endblock %}
{% block contentTitle %}{{ 'Dosya Paylaşımı'|trans }}{% endblock %}

{% block body %}

    <div class="card">
        {{ form_start(form) }}
        <div class="card-body">
            {{ form_widget(form) }}
        </div>
        {{ component('_card_action_footer') }}
        {{ form_end(form) }}
    </div>

{% endblock %}
