{% extends 'layout/admin.html.twig' %}

{% block pageTitle %}{{ 'Takıma Üye Ekle'|trans }}{% endblock %}
{% block contentTitle %}{{ 'Takıma Üye Ekle'|trans }}{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-body">
            {{ form_start(inviteForm) }}
            {{ form_widget(inviteForm) }}
            {{ component('_card_action_footer', { label: "Davet Et"|trans }) }}
            {{ form_end(inviteForm) }}
        </div>
    </div>

{% endblock %}
