{% extends 'layout/admin.html.twig' %}

{% block pageTitle %}{{ 'Google Chrome Kayıtları'|trans }}{% endblock %}
{% block contentTitle %}{{ 'Google Chrome Kayıtları'|trans }}{% endblock %}

{% block pageActions %}
    <a href="{{ path('app_admin_puppeteer_replay_index') }}" class="btn btn-secondary" role="button">{{ 'Listeye Geri Dön'|trans }}</a>
{% endblock %}

{% block body %}

    <div class="card">
        {{ form_start(form) }}
        <div class="card-body">
            {{ form_widget(form) }}
        </div>
        {{ component('_card_action_footer', { label: "Yükle"|trans }) }}
        {{ form_end(form) }}
    </div>

{% endblock %}