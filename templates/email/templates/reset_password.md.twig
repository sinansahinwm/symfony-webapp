{% extends 'layout/email.html.twig' %}

{% block body %}
    {% apply markdown_to_html %}
        # {{ 'Merhaba!'|trans }}

        {{ 'Şifreni sıfırlamak için aşağıdaki bağlantıyı kullanabilirsin.'|trans }}
        {{ 'Butona tıkladığınızda sonuç alamadıysanız aşağıdaki linki kopyalayıp tarayıcınızın adres çubuğuna yapıştırın.'|trans }}

        {{ 'Sıfırlama Bağlantısı:'|trans }} {{ url('app_auth_reset_password_reset', {token: context.resetToken.token}) }}

        {{ 'Bu sıfırlama bağlantısının geçerlilik süresi:'|trans }} **{{ context.resetToken.expirationMessageKey|trans(context.resetToken.expirationMessageData, 'ResetPasswordBundle') }}**
    {% endapply %}
{% endblock %}
