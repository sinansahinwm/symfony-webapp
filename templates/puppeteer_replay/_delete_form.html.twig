<form method="post" action="{{ path('app_puppeteer_replay_delete', {'id': puppeteer_replay.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ puppeteer_replay.id) }}">
    <button class="btn">Delete</button>
</form>
