<!-- userAgreementModal -->
<div class="modal fade" id="userAgreementModal" data-bs-backdrop="static" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <form class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="backDropModalTitle">{{ 'Son Kullanıcı Sözleşmesi'|trans }}</h5>
                <!--
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="{{ 'Kapat'|trans }}"></button>
                -->
            </div>
            <div class="modal-body">
                {{ include('auth/_user_agreement.md.twig') }}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-bs-dismiss="modal">{{ 'Onayla'|trans }}</button>
            </div>
        </form>
    </div>
</div>