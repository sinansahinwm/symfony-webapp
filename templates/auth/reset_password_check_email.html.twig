{% extends 'layout/auth.html.twig' %}

{% block pageTitle %}{{ 'E-Posta Kutunuzu Kontrol Edin'|trans }}{% endblock %}

{% block body %}
    <div class="d-flex col-12 col-lg-5 col-xl-4 align-items-center authentication-bg p-4 p-sm-5">
        <div class="w-px-400 mx-auto">
            <div class="app-brand mb-4">
                <a href="{{ path(layout.admin.logoPath) }}" class="app-brand-link gap-2 mb-2">
                <span class="app-brand-logo demo">
                  {% include 'layout/partials/_logo_svg.html.twig' %}
                </span>
                    <span class="app-brand-text demo h3 mb-0 fw-bold">
                        {{ layout.admin.logoText }}
                    </span>
                </a>
            </div>
            <h4 class="mb-2">{{ 'E-Posta Gönderildi'|trans }} 🔒</h4>
            <p class="mb-4">{{ 'Şifrenizi sıfırlamak için e-posta kutunuzu kontrol edin'|trans }}</p>
            <p>
                {{ 'E-postanızla eşleşen bir hesap varsa, şifrenizi sıfırlamak için kullanabileceğiniz bir bağlantı içeren bir e-posta gönderilmiştir.'|trans }}
                {{ 'Bu sıfırlama bağlantısının geçerlilik süresi:'|trans }} {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </p>
            <p>{{ 'E-posta almazsanız lütfen spam klasörünüzü kontrol edin veya'|trans }} <a href="{{ path('app_auth_reset_password_request') }}">{{ 'yeniden deneyin'|trans }}</a>.</p>
            <div class="text-center">
                <a href="{{ path('app_auth_signin') }}">
                    <i class="bx bx-chevron-left scaleX-n1-rtl"></i>
                    {{ 'Giriş sayfasına git'|trans }}
                </a>
            </div>
        </div>
    </div>
{% endblock %}
